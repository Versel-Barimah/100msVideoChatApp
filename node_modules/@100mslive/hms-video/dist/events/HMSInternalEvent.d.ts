import { EventEmitter2 as EventEmitter } from 'eventemitter2';
export declare class HMSInternalEvent<T> {
    private eventName;
    private eventEmitter;
    constructor(eventName: string, eventEmitter: EventEmitter);
    publish: (event?: T | undefined) => void;
    subscribe: (fn: (event: T) => void | Promise<void>) => void;
    subscribeOnce: (fn: (event: T) => void | Promise<void>) => void;
    unsubscribe: (fn: (event: T) => void | Promise<void>) => void;
    removeAllListeners: () => void;
}
