import { IStore } from '../sdk/store';
import { PeerConnectionType, HMSPeerStats, HMSTrackStats } from '../interfaces/webrtc-stats';
export declare class HMSWebrtcStats {
    private getStats;
    private store;
    private readonly TAG;
    private localPeerID?;
    private peerStats;
    private trackStats;
    constructor(getStats: Record<PeerConnectionType, RTCPeerConnection['getStats'] | undefined>, store: IStore);
    getLocalPeerStats(): HMSPeerStats | undefined;
    getTrackStats(trackId: string): HMSTrackStats | undefined;
    /**
     * @internal
     */
    updateStats(): Promise<void>;
    private updateLocalPeerStats;
    private updateTrackStats;
}
